{
  "name": "aifinpay-casper-demo",
  "version": "1.0.0",
  "description": "AiFinPay x Casper — demo application (bridge, MCP server, dashboard)",
  "scripts": {
    "agent-demo": "node agent-compute-demo.js",
    "dashboard": "node serve-dashboard.js",
    "mcp": "node casper-mcp.mjs",
    "mcp:test": "node test-mcp.mjs",
    "test": "node --test test/*.test.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "casper-js-sdk": "2.15.4",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "prettier": "^3.9.6"
  }
}
