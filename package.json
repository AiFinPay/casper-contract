{
  "scripts": {
    "test": "cargo test --lib --target x86_64-unknown-linux-gnu",
    "build": "cargo build --target wasm32-unknown-unknown --release",
    "clippy": "cargo clippy --target wasm32-unknown-unknown -- -D warnings",
    "fmt": "cargo fmt"
  },
  "devDependencies": {
    "@daochild/agents-config": "^1.2.1"
  }
}
